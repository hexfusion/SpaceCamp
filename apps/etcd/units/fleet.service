[Unit]
Description=fleet daemon

After=etcd.service

Wants=fleet.socket
After=fleet.socket

[Service]
ExecStart=/usr/local/bin/fleetd
Restart=always
RestartSec=10s

[Install]
WantedBy=multi-user.target
~
